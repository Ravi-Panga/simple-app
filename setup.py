from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "It's Wine 0 package",
    author = "Ravi Panga",
    packages = find_packages(),
    license = "MIT"
)